// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model usuario {
  userId       Int         @id @default(autoincrement())
  email        String      @unique
  created_at   DateTime    @default(now())
  password     String
}

model EcoDica {
  id           Int      @id @default(autoincrement())
  categoria    String
  infocards    InfoCard[]

}

model InfoCard {
  id           Int      @id @default(autoincrement())
  titulo       String
  texto        String
  ecodicaId    Int
  ecodicas     EcoDica  @relation(fields: [ecodicaId], references: [id])
}